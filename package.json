{
  "dependencies": {
    "nanoid": "^5.0.6"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "scripts": {
    "build": "npm install --legacy-peer-deps --force && npm install --legacy-peer-deps --force --prefix frontend && npm run build --prefix frontend"
  }
}
