{
  "dependencies": {
    "nanoid": "^5.0.6"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "scripts": {
    "build": "npm install --legacy --force && npm install --force --prefix ../frontend && npm run build --prefix ../frontend"
  }
}
